@model IEnumerable<calendario.Models.Tiposuelo>

@{
    ViewData["Title"] = "Index";
}

@section Scripts{
    <script>
        $(document).ready(function(){
            $('#tbl_tiposuelo').DataTable();
        });
    </script>
}

<h1>Tabla: Tipo de Suelo</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo</a>
</p>
<table id="tbl_tiposuelo" class="display">
    <thead>
        <tr>
            <th>
                Nombre
            </th>
            <th>
                Fecha de Registro
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTipoSuelo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoSuelo" class="btn btn-warning">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoSuelo" class="btn btn-info">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoSuelo" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
