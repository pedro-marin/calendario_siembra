@model IEnumerable<calendario.Models.Tipotecnica>

@{
    ViewData["Title"] = "Index";
}

@section Scripts{
    <script>
        $(document).ready(function(){
            $('#tbl_tipotecnica').DataTable();
        });
    </script>
}

<h1>Tabla: Tipo de Técnica</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo</a>
</p>
<table id="tbl_tipotecnica" class="display">
    <thead>
        <tr>
            <th>
                Nombre
            </th>
            <th>
                Fecha de Registro
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreTipoTecnica)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoTecnica" class="btn btn-warning">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoTecnica" class="btn btn-info">Detalles</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoTecnica" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
